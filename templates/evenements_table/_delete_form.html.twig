<form method="post" action="{{ path('evenements_table_delete', {'id': evenements_table.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenements_table.id) }}">
    <button class="btn">Delete</button>
</form>
