{% extends 'backoffice.html.twig' %}

{% block title %}OffresTable{% endblock %}

{% block body %}
<div style="margin-left:20%;margin-top: 5%">
    <div style="margin-left:25%;margin-top: 5%">
<h1>les coordonnées de l'offre</h1>
    </div>
    <table class="table" style="margin-left: 20%;width: 80%">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offres_table.id }}</td>
            </tr>
            <tr>
                <th>IdOffres</th>
                <td>{{ offres_table.IdOffres }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ offres_table.nom }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ offres_table.dateDebut|date("m/d/Y") }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ offres_table.dateFin |date("m/d/Y")}}</td>
            </tr>
            <tr>
                <th>Sujet</th>
                <td>{{ offres_table.sujet }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ offres_table.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('offres_table_index') }}">back to list</a>

    <a href="{{ path('offres_table_edit', {'id': offres_table.id}) }}">edit</a>

    {{ include('offres_table/_delete_form.html.twig') }}
{% endblock %}
